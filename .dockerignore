# Out of the build context. The build compiles Astro inside the image,
# so it only needs the source code and config; no local artifacts.
.git/
.gitignore
node_modules/
dist/
.astro/
.claude/
tests/
*.md
